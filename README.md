# Course project for Metropolia UAS
- Includes a HTTPS Node server that utilizes PostgreSQL to login and save users and their profiles. 
- Provides access tokens and refresh tokens to users has rotation for refresh tokens
- Includes optional admin features using an admin account with an admin password
